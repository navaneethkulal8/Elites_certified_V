import 'package:flutter/material.dart';

class PointDistribution extends StatefulWidget {
  const PointDistribution({super.key});

  @override
  State<PointDistribution> createState() => _PointDistributionState();
}

class _PointDistributionState extends State<PointDistribution> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Hello world")));
  }
}
